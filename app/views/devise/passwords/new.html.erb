<div class="row justify-content-center p-4">
  <div class="col-12 col-md-6 col-lg-4">
    <div class="form">

      <h2 class="mb-4">Forgot your password?</h2>

      <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
        <%= f.error_notification %>

        <div class="form-inputs">
          <%= f.input :email,
                      required: true,
                      autofocus: true,
                      input_html: { autocomplete: "email", class: 'sciFi-search sciFi-input' } %>
        </div>

        <div class="form-actions">
          <%= f.button :submit, "Send me email", class:'mr-2 sciFi-button' %>
          <%= link_to 'Back', :back, class: 'mt-3 sciFi-button btn--yellow' %>
        </div>
      <% end %>
    </div>
  </div>
</div>

<%= render "devise/shared/links" %>
